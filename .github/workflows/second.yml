name: Second YAML
on:
  workflow_run:
    workflows: ["CI Build"]
    types:
      - completed


jobs:
  if: ${{github.event.workflow_run.conclusion == 'success'}}
  second:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Second
        run: |
          echo "Second YAML File"
          echo "test"