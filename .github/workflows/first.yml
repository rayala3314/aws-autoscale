name: CI Build
on:
  pull_request:
    types:
      - opened
    branches:
      - main
      - feature1
env:
  GITHUB_CONTEXT: ${{ toJson(github)}}
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Echo
        run: |
          echo "First YAML File"
          echo "$GITHUB_CONTEXT"
          echo "Test"
