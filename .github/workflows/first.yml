name: CI Build
on:
  pull_request:
    types:
      - opened
    branches:
      - main
      - feature1
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Echo
        run: echo "First YAML File"
